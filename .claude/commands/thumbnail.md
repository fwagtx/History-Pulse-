Produce thumbnail concepts for a History Pulse video.

Slug given by the human: $ARGUMENTS

1. Read `content/01_research/<slug>/notes.md` and `content/03_seo/<slug>/seo.md` for context
   on the topic and chosen title.
2. Write 2–3 thumbnail concepts to `content/04_thumbnails/<slug>/concepts.md`, each with:
   - Composition and focal image description
   - Exact overlay text (3–5 words max, high contrast)
   - Color/contrast direction
   - Emotion/expression if a person or face is shown
   - Which free tool it's designed for (Canva free tier, Photopea, or GIMP)
3. For the recommended concept, also produce an SVG or HTML mockup showing the rough layout
   (text placement, focal point, color blocking) so the human can see it before building the
   real thumbnail. Save it as `content/04_thumbnails/<slug>/mockup.svg` (or `.html`).
4. Never use real copyrighted images, film stills, or branded logos in the mockup — describe
   the desired real image in words for the human to source from Wikimedia Commons, Library of
   Congress, or similar, rather than fabricating a licensed image.
5. End by asking: "Thumbnail concepts saved for `<slug>`. Ready for `/review <slug>`?"
